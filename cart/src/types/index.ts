export type ReqLogin = {
    username: string;
    password: string;
};

export type CartItem = {
    description: string
    id: number
    image: number
    longDescription: string
    name: string
    price: number
    quantity: number
}